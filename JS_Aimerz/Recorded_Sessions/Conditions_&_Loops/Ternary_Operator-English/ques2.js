// Question 2 :
// * Determine the larger of two numbers.

// Input : Two numbers(number)
// Output : A message

let a = 56;
let b = -1;

const output = a === b ? `Both the numbers are equal.` : a > b ? `${a} is greater than ${b}.` : `${b} is greater than ${a}.`;

console.log(output);

// -----------------------------------------------------------------------------------------------

