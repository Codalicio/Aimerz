// Question 10 : 

// * Check if an array is empty.

let arr = [1];

const isArrayEmpty = arr.length === 0 ? 'Empty Array' : 'Non-empty array';

console.log(isArrayEmpty);

// ----------------------------------------------------------------------------------

