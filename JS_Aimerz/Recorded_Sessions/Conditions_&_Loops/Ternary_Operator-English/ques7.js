// Question 7 :

// * Apply a discount based on purchase amount.

let purchaseAmount = 100;

let discount = purchaseAmount >= 100 ? 0.1 : 0;

let finalPriceAfterDiscount = purchaseAmount - (purchaseAmount * discount);

console.log(finalPriceAfterDiscount);

// --------------------------------------------------------------------------------