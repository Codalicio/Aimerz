// Question 1 :

// * Check if a number is even or odd?

// Input : A number
// Output : A message

let numberToCheck = 909765332;

const output = numberToCheck === 0 ? `The number ${numberToCheck} is neither even nor odd` : numberToCheck % 2 === 0 ? `The number ${numberToCheck} is even.` : `The number ${numberToCheck} is odd.`;

console.log(output);

// ------------------------------------------------------------------------------------

